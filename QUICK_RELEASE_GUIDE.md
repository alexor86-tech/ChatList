# 🚀 Быстрая инструкция по публикации

## Шаг 1: Подготовка

```bash
# Убедитесь, что версия актуальна
cat version.py

# Соберите файлы
source venv/bin/activate
pyinstaller ChatList.spec
./build_deb.sh
```

## Шаг 2: Подготовка релиза

```bash
# Используйте скрипт для подготовки
./scripts/prepare_release.sh
```

Скрипт создаст директорию `release_v{VERSION}/` с:
- Исполняемым файлом
- .deb пакетом
- Шаблоном Release Notes

## Шаг 3: Создание тега и релиза

```bash
# Получите версию
VERSION=$(python3 -c "import version; print(version.__version__)")

# Создайте и отправьте тег
git tag -a "v${VERSION}" -m "Release v${VERSION}"
git push origin "v${VERSION}"
```

## Шаг 4: Создание Release на GitHub

1. Перейдите: `https://github.com/YOUR_USERNAME/ChatList/releases/new`
2. Выберите тег `v{VERSION}`
3. Заголовок: `ChatList v{VERSION}`
4. Вставьте содержимое из `release_v{VERSION}/RELEASE_NOTES.md`
5. Загрузите файлы из `release_v{VERSION}/`
6. Нажмите **Publish release**

## Шаг 5: Настройка GitHub Pages

1. Перейдите: `Settings` → `Pages`
2. Source: `Deploy from a branch`
3. Branch: `main` / Folder: `/docs`
4. Сохраните

Через несколько минут сайт будет доступен по адресу:
`https://YOUR_USERNAME.github.io/ChatList/`

## ⚠️ Не забудьте

- [ ] Заменить `YOUR_USERNAME` в `docs/index.html`
- [ ] Заполнить Release Notes
- [ ] Протестировать сборки перед публикацией

## 📖 Подробная инструкция

См. `GITHUB_RELEASE_GUIDE.md` для детальной информации.

